{
  "name": "wallpaper-render-service",
  "version": "1.0.0",
  "private": true,
  "description": "Wallpaper image rendering service",
  "scripts": {
    "dev": "node server/app.js",
    "start": "node server/app.js",
    "test": "node --test tests/**/*.test.js",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.51.1"
  }
}
